# gimme - A simple, native Node.JS HTTP request module

gimme makes Node.js http/s connection requests easy without the need for any external dependancies.
It supports HTTPS and follows redirects by default.
